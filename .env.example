# Environment Variables Example
# Copy this file to .env and adjust the values

# ============================================================================
# FRONTEND Environment Variables
# ============================================================================

# PocketBase Backend URL (REQUIRED - must be accessible from browser)
# Production with Caddy: https://api.ducworld.com
# Local development: http://127.0.0.1:8090
NEXT_PUBLIC_POCKETBASE_URL=https://api.ducworld.com

# Enable/Disable Add to Cart functionality
NEXT_PUBLIC_ENABLE_ADD_TO_CART=true

# PocketBase Admin Credentials (for automated setup)
PB_ADMIN_EMAIL=admin@ducworld.com
PB_ADMIN_PASSWORD=admin123@

# ============================================================================
# BACKEND/PocketBase Environment Variables
# ============================================================================

# Stripe Configuration (if using payments)
STRIPE_API_KEY=<your_stripe_api_key>
STRIPE_WEBHOOK_SECRET=<your_stripe_webhook_secret>

# Production Mode
isProduction=production

# Frontend URL (used by backend for CORS and redirects)
FRONTEND_URL=https://ducworld.com

# ============================================================================
# IMPORTANT NOTES FOR DOCKER + CADDY DEPLOYMENT
# ============================================================================
#
# 1. NEXT_PUBLIC_POCKETBASE_URL must be the PUBLIC URL accessible from browsers
#    NOT the internal Docker network name!
#
# 2. For Caddy reverse proxy setup:
#    - Frontend: https://ducworld.com -> http://frontend:3000
#    - Backend:  https://api.ducworld.com -> http://backend:8080
#
# 3. Make sure to rebuild containers after changing these values:
#    docker-compose down && docker-compose up --build -d
#
# 4. PocketBase CORS: Ensure your backend allows requests from ducworld.com
#

